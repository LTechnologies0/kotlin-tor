# WinSW service definition for kotlin-tor (Windows).
# Install: copy beside WinSW.exe as kotlin-tor.xml, then `kotlin-tor.exe install`
id: see packaging/windows/kotlin-tor.xml generated content in org.kotlintor.os.WinService

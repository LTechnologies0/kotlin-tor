package org.kotlintor.or

/**
 * Former multi-struct mirror file. Types now live in dedicated primaries under
 * `org.kotlintor.or` (e.g. [PackedCell], [RelayMsg], [TorVersion]).
 *
 * Inventory elevations use STRUCT_HINTS + TYPE_SEED_DEPTH pointing at those files.
 */
@Deprecated("Types split into dedicated org.kotlintor.or.* files")
object OrStructMirrors
